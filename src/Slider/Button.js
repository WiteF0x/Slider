import React from 'react';
import {
  View,
  Text,
  TouchableOpacity,
} from 'react-native';
import styles from './styles';

const Button = () => {

  return (
    <View style={styles.button}>
    </View>
  );
};

export default Button;
