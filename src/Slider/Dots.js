import React from 'react';
import { View } from 'react-native';

import styles from './styles';

const Dots = ({ count, scrollX }) => {

  return (
    <View style={styles.dots}>
    </View>
  );
};

export default Dots;
